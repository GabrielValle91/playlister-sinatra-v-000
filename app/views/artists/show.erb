<ul>
  <% @artist.songs.each do |song| %>
    <li><a href="/songs/<%= song.slug %>"><%= songs.name %></a></li>
  <% end %>
</ul>

<ul>
  <% @artist.genres.each do |genere| %>
    <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
  <% end %>
</ul>
